# Use when Railway Root Directory = "backend"

[build]
builder = "RAILPACK"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/_health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
